# git-demo

#### 初始化项目

```bash
mkdir git-demo && cd git-demo
echo "# git-demo" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/isxcode/git-demo.git
git push -u origin main
```
